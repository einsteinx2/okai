# Makefile autogenerated by premake
# Don't edit this file! Instead edit `premake.lua` then rerun `make`
# Options:
#   CONFIG=[Release]

ifndef CONFIG
  CONFIG=Release
endif

export CONFIG

.PHONY: all clean nyx

all: nyx

Makefile: premake.lua
	@echo ==== Regenerating Makefiles ====
	@premake --file $^ --os linux --target gnu

nyx:
	@echo ==== Building nyx ====
	@$(MAKE) --no-print-directory -C . -f nyx.make

clean:
	@$(MAKE) --no-print-directory -C . -f nyx.make clean
